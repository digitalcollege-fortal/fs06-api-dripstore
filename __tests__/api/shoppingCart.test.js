// const makeRequest = require('../../infrastructure/makeRequest')

test('Garante que o endpoint para buscar todos os items do carrinho funciona', async () => {
    // let dados = await makeRequest('http://localhost:8000/carrinho');

    // expect(typeof dados).toBe('object');
    // expect(dados[0].id).toBe('number');
});

test('Garante que o endpoint para buscar todos os items do carrinho funciona', async () => {
    // let dados = await makeRequest('http://localhost:8000/carrinho/1');

    // expect(typeof dados).toBe('object');
    // expect(dados[0].valor_total).toBe(100.90);
});
